---
interface Props {
	text: string
	className?: string
}

const { text, className } = Astro.props
---

<div class=`hand-drawn-shadow mb-4 rounded-2xl border-4 border-black bg-white p-2 ${className}`>
	<h1 class="font-heading-1 text-4xl">{text}</h1>
</div>
